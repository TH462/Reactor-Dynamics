/* Deploy stamp. Placeholder in the repo; the Vercel build step overwrites this
 * file from VERCEL_GIT_COMMIT_SHA (WEBSITE_SPEC.md §8). Footer scripts read it. */
window.RD_VERSION = 'alpha · dev build';
