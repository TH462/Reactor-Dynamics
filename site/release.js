/* The player-facing release version, shown next to the logo in the control room.
 *
 * Hand-edited — this is NOT generated. Bump it in the SAME change as the new
 * changelog.html entry (see CLAUDE.md, "Website changelog & version numbers":
 * Alpha X.Y.Z = Platform . Feature . Refinement). The top entry of
 * changelog.html and this string must always agree.
 *
 * Distinct from window.RD_VERSION (site/version.js), which is the git SHA
 * Vercel stamps at deploy time and is only used for feedback telemetry. */
window.RD_RELEASE = "Alpha 1.8.2";
